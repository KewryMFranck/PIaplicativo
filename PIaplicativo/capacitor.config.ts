import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PIaplicativo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
